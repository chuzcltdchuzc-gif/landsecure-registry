{
  "name": "JobQueue",
  "type": "object",
  "properties": {
    "job_id": {
      "type": "string",
      "description": "Human-readable job identifier"
    },
    "job_type": {
      "type": "string",
      "enum": [
        "fraud_scoring",
        "gis_validation",
        "pdf_generation",
        "bulk_import",
        "report_export",
        "backup",
        "duplicate_scan",
        "evidence_hashing",
        "archive_import",
        "verification_report",
        "ocr_processing",
        "qr_certificate_generation",
        "pdf_certificate_generation",
        "confidence_recalculation",
        "notification"
      ],
      "description": "Type of background job"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "queued",
        "processing",
        "running",
        "completed",
        "failed",
        "retrying",
        "cancelled"
      ],
      "default": "pending"
    },
    "entity_type": {
      "type": "string",
      "description": "Target entity type (e.g. LandVaultParcel, EvidenceVault)"
    },
    "entity_id": {
      "type": "string",
      "description": "Target entity ID"
    },
    "created_by": {
      "type": "string",
      "description": "User email who created the job"
    },
    "payload": {
      "type": "string",
      "description": "JSON string of job input parameters"
    },
    "result": {
      "type": "string",
      "description": "JSON string of job output/result"
    },
    "error_message": {
      "type": "string",
      "description": "Error details if job failed"
    },
    "attempts": {
      "type": "number",
      "default": 0,
      "description": "Number of attempts made (including current)"
    },
    "max_attempts": {
      "type": "number",
      "default": 3,
      "description": "Maximum attempts before marking as FAILED"
    },
    "idempotency_key": {
      "type": "string",
      "description": "Unique key to prevent duplicate job creation for the same operation"
    },
    "priority": {
      "type": "string",
      "enum": [
        "low",
        "normal",
        "high",
        "critical"
      ],
      "default": "normal"
    },
    "progress_pct": {
      "type": "number",
      "default": 0,
      "description": "Progress percentage 0-100"
    },
    "started_at": {
      "type": "string",
      "format": "date-time"
    },
    "completed_at": {
      "type": "string",
      "format": "date-time"
    },
    "result_reference": {
      "type": "string",
      "description": "Reference to job result (parcel_id, file_uri, certificate_url)"
    }
  },
  "required": [
    "job_type"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": {
          "$in": [
            "super_admin",
            "surveyor_general",
            "compliance_officer",
            "licensed_surveyor",
            "surveyor_partner",
            "field_agent"
          ]
        }
      }
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": {
              "$in": [
                "super_admin",
                "surveyor_general",
                "compliance_officer",
                "licensed_surveyor",
                "surveyor_partner"
              ]
            }
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": {
          "$in": [
            "super_admin",
            "surveyor_general",
            "compliance_officer"
          ]
        }
      }
    },
    "delete": {
      "user_condition": {
        "role": "super_admin"
      }
    }
  }
}